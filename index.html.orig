<!doctype html>
<html lang="en">
<head>
	<meta charset="utf-8">

	<title>Pip-Boy 3000 Mark IV</title>
	<meta name="description" content="Pip-Boy 3000 Mark IV">
	<meta name="author" content="thunderblaster">

	<link rel="stylesheet" href="css/styles.css?v=1.0">
	<script src="js/jquery.min.js"></script>
	<script src="js/vue.js"></script>
	<script defer src="js/app.js"></script>
</head>
<body>
	<div id="vue-app">
		<div class="menu-system">
			<div class="top-menu">
				<ul>
					<li v-for="(menu, index) in menus">
						<button v-if="activeMenu===index" class="top-menu-button active-menu-button menu-button">{{menu.name}}</button>
						<button v-else @click="switchMenu(index)" class="top-menu-button menu-button">{{menu.name}}</button>
					</li>
				</ul>
			</div>
			<div v-if="menus[activeMenu].submenus.length>0" :class="menus[activeMenu].submenuClasses">
				<ul>
<<<<<<< HEAD
					<li v-for="(submenu, index) in visibleSubMenus">
							<button v-if="activeSubMenu===index" autofocus class="active-submenu-button submenu-button menu-button">{{submenu.name}}</button>
							<button v-else @click="activeSubMenu=index" class="submenu-button menu-button">{{submenu.name}}</button>
					</li>
=======
					<li><button v-if="activeSubMenu>1" @click="activeSubMenu=activeSubMenu-2" class="submenu-button menu-button submenu-fade">{{menus[activeMenu].submenus[activeSubMenu-2].name}}</button></li>
					<li><button v-if="activeSubMenu>0" @click="activeSubMenu=activeSubMenu-1" class="submenu-button menu-button">{{menus[activeMenu].submenus[activeSubMenu-1].name}}</button></li>
					<li><button v-if="menus[activeMenu].submenus.length>0" class="submenu-button menu-button active-submenu-button">{{menus[activeMenu].submenus[activeSubMenu].name}}</button></li>
					<li><button v-if="activeSubMenu+1<menus[activeMenu].submenus.length" @click="activeSubMenu=activeSubMenu+1" class="submenu-button menu-button">{{menus[activeMenu].submenus[activeSubMenu+1].name}}</button></li>
					<li><button v-if="activeSubMenu+2<menus[activeMenu].submenus.length" @click="activeSubMenu=activeSubMenu+2" class="submenu-button menu-button submenu-fade">{{menus[activeMenu].submenus[activeSubMenu+2].name}}</button></li>
>>>>>>> submenu-view
				</ul>
			</div>
		</div>
		<div class="body">
			<p v-if="menus[activeMenu].submenus.length>0">You are viewing the {{menus[activeMenu].submenus[activeSubMenu].name}} screen.</p>
			<p v-else>You are viewing the {{menus[activeMenu].name}} screen.</p>
		</div>
	</div> <!-- /app -->
</body>
